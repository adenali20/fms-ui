apiVersion: v1
kind: Service
metadata:
  name: {{ include "fms-ui.fullname" . }}
  labels:
    app: {{ include "fms-ui.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "fms-ui.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
